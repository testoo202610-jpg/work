# Asset Licenses

All game visuals are generated at runtime with Phaser graphics primitives. No commercial-game artwork, audio, maps, names, characters, or code are included.

The interface font is loaded from Google Fonts (`Cairo`) at runtime. Replace it with a local or appropriately licensed font if offline distribution is required.

No audio assets are currently bundled; the audio layer is intentionally deferred until original generated cues are added.
